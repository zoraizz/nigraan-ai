// TODO: not implemented yet — scaffold placeholder, safe to call.
//
// Intended signature:
//   useAidPriority(districts) -> { ranking, scoring, loading, error, refetch }
// Should wrap rankPriority (../api/aidPriority.js); `ranking` is the
// ranked_districts array, `scoring` the transparency metadata block.
export function useAidPriority() {
  console.warn('useAidPriority: TODO stub — not implemented yet')
  return { ranking: [], scoring: null, loading: false, error: null, refetch: () => {} }
}
