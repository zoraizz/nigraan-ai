// TODO: not implemented yet — scaffold placeholder, safe to call.
//
// Intended signature:
//   useRiskData(district) -> { data, loading, error, refetch }
// Should wrap predictRisk (../api/riskFlag.js) and expose the /predict-risk
// response body as `data`.
export function useRiskData() {
  console.warn('useRiskData: TODO stub — not implemented yet')
  return { data: null, loading: false, error: null, refetch: () => {} }
}
