// Placeholder — renders children unconditionally. When real auth lands, this
// is the single place to add the isAuthenticated check and a redirect to a
// sign-in route; nothing else needs to change.
export default function ProtectedRoute({ children }) {
  return children
}
